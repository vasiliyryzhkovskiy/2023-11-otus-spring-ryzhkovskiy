spring:
  application:
    title: otus-homework
    version: hw07-spring-data
  datasource:
    url: jdbc:h2:mem:maindb
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
      show-sql: false

#  h2:
#    console:
#      path: /h2-console
#      settings:
#        web-allow-others: true