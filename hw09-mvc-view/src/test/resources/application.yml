spring:
  application:
    title: otus-homework
    version: hw09-mvc-view
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  sql:
    init:
      mode: always
      data-locations: data.sql
logging:
  level:
    root: info